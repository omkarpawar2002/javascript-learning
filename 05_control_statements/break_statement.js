/*
break statements :- 
        break statements are used to immediatly terminate the block of code.
        break keyword is used to exits from the loops.
*/

for (let i = 0; i < 11; i++) {
    if (i == 6) {
        break;
    }
    console.log(i);
}